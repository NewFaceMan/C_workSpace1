#include <stdio.h>
int main(int argc, char const *argv[])
{
    int a, b;
    int start;
    int end;
    scanf("%d %d", &a, &b);
    for (int i = a; i <= b; i++)
    {
        if (b > a)  
        {
            
        }
        
        printf("%d", i);
    }
    
    //8하고 2넣으면 왜 안 되는지 알아내기
    return 0;
}
